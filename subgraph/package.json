{
  "name": "@arcasset/subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:studio": "graph deploy --node https://api.studio.thegraph.com/deploy/ arcasset",
    "create:local": "graph create --node http://localhost:8020/ arcasset",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arcasset",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.38.1"
  },
  "packageManager": "bun@1.4.2"
}
